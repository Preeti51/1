import React from "react";
import { Header, Slider, Directors, Working } from "../components";

const contactus = () => {
  return (
    <React.Fragment>
      <Header />
      <Slider />
      <Directors />
      <Working />
    </React.Fragment>
  );
};

export default contactus;
