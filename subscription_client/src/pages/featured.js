import React from "react";
import { Header, Slider, Featured } from "../components";

const featured = () => {
  return (
    <React.Fragment>
      <Header />
      <Slider />
      <Featured />
    </React.Fragment>
  );
};

export default featured;
