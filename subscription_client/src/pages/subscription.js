import React from "react";
import { Header, Slider } from "../components";

const subscription = () => {
  return (
    <React.Fragment>
      <Header />
      <Slider />
    </React.Fragment>
  );
};

export default subscription;
