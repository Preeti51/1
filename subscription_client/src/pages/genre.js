import React from "react";
import { Header, Slider, MaterialData } from "../components";
const genre = () => {
  return (
    <React.Fragment>
      <Header />
      <Slider />
      <MaterialData />
    </React.Fragment>
  );
};

export default genre;
